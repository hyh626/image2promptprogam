Read program.md and any prior run reports under outputs/runs/. You
are the driver agent. The harness is already built — you only edit
configs and prompt-strategy inputs, and append findings to each
run's report.md.

For each experiment: re-read the latest run report, write a
one-sentence hypothesis, edit the structured-prompt schema or
search config, run the runner CLI per program.md, and read the
per-component breakdown (whole-image embedding / region embedding
/ VLM judge), not just the composite. Use beam search defaults
(B=3, K=4 mutations, max_iterations=8) and re-evaluate finalists
across 3+ seeds before declaring an improvement.

Aim for at least 20 experiments this session. Prioritize
qualitatively different strategies (structured-vs-freeform,
critique-guided vs blind mutation, region-aware vs whole-image
ranking, prompt length, negative prompts) over local optimization
of one approach. Record which schema field changed and why in
`mutation_rationale` for every candidate. Do not modify core code
under src/, scoring, API wrappers, target images, or generator
settings (model, resolution, seed policy). Do not chase a single
lucky seed.
