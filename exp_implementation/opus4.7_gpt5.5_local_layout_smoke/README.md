# Image Prompt Autoresearch

Bootstrap harness for the image-to-prompt-to-image autoresearch loop described in `program.md`.

Place the fixed benchmark images in `eval_data/images/eval/` and the held-out
validation images in `eval_data/images/val/`.
