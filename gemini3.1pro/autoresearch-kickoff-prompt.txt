Read program.md. You are running the Vision-Ratchet loop. The
harness (main.py) is already built — do not modify it, the
patch-based scoring (2x2 grid, mean cosine similarity), the
target_image.png, or the model identifiers. Your only editable
surface between runs is prompt.txt; during a run the harness
manages prompt.txt itself via the ratchet (keep on improvement,
revert on regression).

Pre-run checklist: confirm target_image.png exists in the working
directory, confirm prompt.txt holds a foundational zero-shot
prompt (one or two sentences describing the subject, style, and
lighting), and confirm GEMINI API credentials are reachable.

Start the loop with `python main.py --iterations 100`. The
harness will: read prompt.txt, generate an image, compute
patch-based similarity against target_image.png, decide
keep-or-revert, then ask the inner VLM to identify ONE specific
visual discrepancy and rewrite the prompt to fix it. Repeat for
the requested iteration count.

When the run finishes, inspect history.log for the score
trajectory and the kept/reverted ratio. Report: starting score,
best score, final prompt.txt, the iteration where the largest
jump occurred, and whether the loop plateaued. Then propose ONE
concrete next action — extend iterations, restart from a stronger
baseline prompt, or swap target_image.png for a different
reproduction stress test.
